{
  "name": "reality-lock-backend",
  "version": "0.1.0",
  "description": "Verification & storage backend for Reality Lock (Phase 1 skeleton).",
  "license": "MIT",
  "private": true,
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "validate:schema": "node scripts/validate-schema.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "asn1js": "^3.0.10",
    "canonicalize": "^2.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^5.1.0",
    "express-rate-limit": "^8.6.1",
    "helmet": "^8.1.0",
    "morgan": "^1.10.0",
    "pkijs": "^3.4.0",
    "rate-limit-redis": "^6.0.1",
    "redis": "^6.2.0"
  }
}
