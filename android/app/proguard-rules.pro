# Reality Lock — ProGuard/R8 rules.
# Shrinking/obfuscation is disabled until Phase 6 (isMinifyEnabled = false).
# When enabled, add keep rules here for reflection-based libraries as needed
# (e.g. Retrofit/Gson model classes, Room, Tink). Left intentionally empty for
# the Phase-1 skeleton.
